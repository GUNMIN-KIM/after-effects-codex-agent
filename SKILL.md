# After Effects Codex Skill

## 목적

이 skill은 Codex가 `mcp-aftereffects`를 통해 Adobe After Effects 프로젝트를 안전하게 조사하고 수정하도록 한다. 해당 MCP는 실행 계층이고 이 문서는 **판단 순서, 품질 기준, 변경 통제, 검수 방식**을 담당한다. AE의 현재 상태를 보지 않고 JSX나 임의의 수치를 추측해 실행하지 않는다.

## 표준 워크플로

### 1. UNDERSTAND
사용자의 요청을 결과물 기준으로 다시 정리한다. 대상 프로젝트, 대상 컴포지션, 변경 범위, 유지할 요소, 출력 형식, 전체 길이, fps, 해상도, 색상 공간, 납품 경로를 확정한다. “빠르게”, “꾸덕하게”, “전문적으로” 같은 표현은 관찰 가능한 값이나 비교 기준으로 바꾼다.

### 2. INSPECT
`ae_project_info`, `ae_comp_info`, `ae_layer_info`, `ae_context`를 현재 MCP catalog에서 찾아 읽는다. 소스 경로, footage 해석, 레이어 순서, in/out, parent, matte, blending mode, effect stack, keyframe 존재 여부를 기록한다. 읽기 결과가 부족하면 mutation하지 않고 필요한 조회를 추가한다.

### 3. PLAN
수정 전 계획을 다음 표로 만든다.

| 대상 | 현재 상태 | 변경 | 유지 | 검수 방법 |
|---|---|---|---|---|
| 컴포지션 | 실제 조회값 | 필요한 값만 | fps·해상도 등 | comp 재조회 |
| 레이어 | stable ID와 역할 | 소스·효과·키프레임 | 이름·순서 등 | layer 재조회 |
| 출력 | 현재 설정 | 새 출력 항목 | 원본 출력 | 렌더 프레임 |

### 4. PROTECT
원본 프로젝트를 별도 보존하고, 새 프로젝트 또는 새 버전으로 저장한다. 작업 폴더와 출력 폴더를 분리한다. 변경 전 프로젝트명·파일 경로·레이어 목록을 기록한다. 새 파일 생성이 불가능하면 작업을 중지하고 사용자에게 경로를 묻는다.

### 5. EXECUTE
`ae_catalog`로 현재 사용 가능한 operation을 확인한 뒤 최소 단위로 실행한다. 한 번에 큰 mutation을 몰아넣지 않는다. 각 mutation 뒤에 관련 상태를 다시 읽는다. 레이어 이름이 아닌 stable layer ID를 우선 사용하며, 이름이 겹치면 실행하지 않는다.

### 6. RE-INSPECT
소스 교체, 매트 연결, effect 추가, keyframe 작성, precompose 이후에는 반드시 해당 레이어와 컴포지션을 다시 조회한다. 결과가 계획과 다르면 다음 작업을 하지 말고 repair plan을 만든다.

### 7. RENDER FRAME → VISUAL QA
`ae_render_frame`으로 첫 프레임, 변화 직전, 변화 중간, 변화 종료 직후, 마지막 프레임을 캡처한다. 검수는 데이터 조회와 실제 화면을 함께 사용한다. 화면이 정상이어도 source mapping·fps·output setting을 확인하지 않으면 완료로 보지 않는다.

### 8. REPAIR → FINAL QC
문제는 소스, 시간축, 매트, 색상, 효과, 출력 순서로 분리해 한 원인씩 수정한다. 수정을 반복할 때마다 같은 검수 프레임을 다시 렌더링한다. 최종 QC는 `references/qc-render.md`의 gate를 통과해야 한다.

### 9. SAVE
새 `.aep`, 검수용 영상, 변경 로그를 함께 저장한다. 최종 보고에는 프로젝트 경로, 컴포지션명, 길이, fps, 변경한 것, 유지한 것, 미해결 사항을 적는다.

## 품질 기본값

매트는 알파 채널과 Luma 정보를 혼동하지 않는다. 로토 경계에는 필요 최소한의 feather와 choke만 사용한다. 컬러는 색상 공간을 확인한 뒤 노출·화이트밸런스·대비·스타일 순서로 조정한다. 글로우는 최종 VFX나 그래픽에 선택적으로 적용하고 피사체 경계를 부풀리지 않는다. 키프레임은 시간코드로 명시하고 transition duration을 별도로 검수한다.

## 완료 조건

사용자 요구사항과 실제 결과가 일치하고, 기존 프로젝트가 보존되며, 소스가 offline이 아니고, 모든 요청된 프레임에서 매트·색감·타이밍·렌더 결과가 정상이어야 한다. “렌더 완료”나 “AE에 열어둠”만으로는 완료로 간주하지 않는다.
